import React from 'react'
import Logo from '../../img/logo.png'

const header = () => {
    return (
        <header className="center">
            <img src={Logo} alt='' />  
        </header>
    )
}

export default header
